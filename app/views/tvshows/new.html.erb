<h1>New tvshow</h1>

<%= form_for(@tvshow, :url => new_tvshow_path(), :method => :get ) do |f| %>
  <% if @tvshow.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tvshow.errors.count, "error") %> prohibited this tvshow from being saved:</h2>

      <ul>
      <% @tvshow.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= label_tag "Suche nach dem Titel" %><br />
    <%= text_field_tag :title %>
  </div>

  <div class="actions">
    <%= f.submit :value => "Suche"  %>
  </div>
<% end %>
<%= link_to 'Back', tvshows_path %>
